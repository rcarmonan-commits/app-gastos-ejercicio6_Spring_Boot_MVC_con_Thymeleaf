# Configuracion Base de Datos MySQL (Remota AlwaysData)
spring.datasource.url=jdbc:mysql://mysql-app-gastos-ejercicio6.alwaysdata.net:3306/app-gastos-ejercicio6_bd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=app-gastos-ejercicio6
spring.datasource.password=R7512510045

# Configuracion Spring Data JPA e Hibernate
# 'update' creara las tablas si no existen, basado en las entidades
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Inicializacion de datos (para cargar schema.sql o data.sql)
# spring.sql.init.mode=always

# Configuracion Thymeleaf
spring.thymeleaf.cache=false

# Configuracion Correo SMTP (Debe configurarse con credenciales reales para funcionar)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tu_correo@gmail.com
spring.mail.password=tu_contraseña_de_aplicacion
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
